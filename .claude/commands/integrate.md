# 전체 파이프라인 통합 점검

YouTube URL → PPT 생성까지 전체 흐름을 점검합니다.

## 작업

1. `app/routers/slides.py`의 전체 흐름 검증:
   - URL 파싱 → 메타데이터 추출 → 자막 추출 → 청크 분할 → AI 요약 → PPT 생성
2. 각 서비스 간 데이터 흐름(인터페이스)이 올바른지 확인
3. 에러 처리 경로 점검 (400, 404, 500)
4. `app/models/schemas.py`의 요청/응답 모델과 실제 데이터 일치 여부 확인
5. 발견된 문제 수정
